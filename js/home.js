const loaderScreen=document.querySelector("#loader-screen");const loaderPercent=document.querySelector("#loader-percent");let progress=0;const loaderInterval=setInterval(function(){progress+=5;loaderPercent.textContent=progress+"%";if(progress>=100){clearInterval(loaderInterval);loaderScreen.classList.add("hide")}},30);
const menuButton=document.querySelector("#menu-button");const hamburgerMenu=document.querySelector("#hamburger-menu");const profileButton=document.querySelector("#profile-button");const profileMenu=document.querySelector("#profile-menu");
menuButton.addEventListener("click",function(){hamburgerMenu.classList.toggle("hidden");profileMenu.classList.add("hidden")});
profileButton.addEventListener("click",function(){profileMenu.classList.toggle("hidden");hamburgerMenu.classList.add("hidden")});
document.addEventListener("click",function(event){if(!event.target.closest("#menu-button")&&!event.target.closest("#hamburger-menu"))hamburgerMenu.classList.add("hidden");if(!event.target.closest("#profile-button")&&!event.target.closest("#profile-menu"))profileMenu.classList.add("hidden")});
const searchInput=document.querySelector("#game-search");const gameCards=document.querySelectorAll(".game-card");const noResults=document.querySelector("#no-results");searchInput.addEventListener("input",function(){const search=searchInput.value.toLowerCase();let visible=0;gameCards.forEach(function(card){const match=card.dataset.title.toLowerCase().includes(search);card.style.display=match?"block":"none";if(match)visible++});noResults.style.display=visible===0?"block":"none"});