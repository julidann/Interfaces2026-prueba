const loaderScreen=document.querySelector("#loader-screen");const loaderPercent=document.querySelector("#loader-percent");let progress=0;const timer=setInterval(function(){progress+=5;loaderPercent.textContent=progress+"%";if(progress>=100){clearInterval(timer);loaderScreen.classList.add("hide")}},25);
const menuButton=document.querySelector("#menu-button");const hamburgerMenu=document.querySelector("#hamburger-menu");const profileButton=document.querySelector("#profile-button");const profileMenu=document.querySelector("#profile-menu");
menuButton.addEventListener("click",function(e){e.stopPropagation();hamburgerMenu.classList.toggle("hidden");profileMenu.classList.add("hidden")});
profileButton.addEventListener("click",function(e){e.stopPropagation();profileMenu.classList.toggle("hidden");hamburgerMenu.classList.add("hidden")});
document.addEventListener("click",function(e){if(!e.target.closest("#hamburger-menu"))hamburgerMenu.classList.add("hidden");if(!e.target.closest("#profile-menu"))profileMenu.classList.add("hidden")});
const searchInput=document.querySelector("#game-search");const gameCards=document.querySelectorAll(".game-card");const noResults=document.querySelector("#no-results");searchInput.addEventListener("input",function(){const text=searchInput.value.toLowerCase();let visible=0;gameCards.forEach(function(card){const match=card.dataset.title.toLowerCase().includes(text);card.style.display=match?"block":"none";if(match)visible++});noResults.style.display=visible?"none":"block"});
document.querySelectorAll(".pagination button").forEach(function(button){button.addEventListener("click",function(){if(!isNaN(Number(button.textContent))){document.querySelectorAll(".pagination button").forEach(function(b){b.classList.remove("selected")});button.classList.add("selected")}})});